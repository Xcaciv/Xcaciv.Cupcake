# Verification findings for S11-open-questions.md

25 finding(s). Apply every one.

## 1. [CRITICAL] §11.2 register header (S11-open-questions.md:38) and §1 Executive Summary (S01-executive-summary.md:61)

**Problem:** The claim "Fifty distinct quirks were catalogued" (repeated in §1 as "**50 are catalogued** (§11.2)") is materially wrong. Grepping "QUIRK" across §6 and all eleven §7 subsection files yields, after merging near-duplicates and consequence-of chains, at least 81 distinct quirks, each separately QUIRK-tagged with its own observable consequence and its own keep-or-fix decision. The register covers 50 of them; 31 are documented in the body and absent from the table. Because §10 Phase 0's deliverable is "a signed-off disposition for every entry in the §11.2 quirk register", every omitted row is a decision the clone team will never be asked to make — including four that make the shipped artefact non-functional and three that are supply-chain relevant. §7.9 FR-9.31 even flags its own omission in the body text: "QUIRK Q14 (newly identified in this subsection; not in the shared quirk register)".

**Required fix:** Change §11.2's opening sentence to "Eighty-one distinct quirks were catalogued." and §1's sentence to "**81 are catalogued** (§11.2)." Add the 31 rows given in the findings below (Q-51 … Q-81), and delete the parenthetical "(newly identified in this subsection; not in the shared quirk register)" from §7.9 FR-9.31 once Q-56 exists.

---

## 2. [CRITICAL] §11.2 — no row for the shipping Release profile; body at §7.3 FR-3.77 (QUIRK-10), §7.5 FR-5.55, §7.6 FR-6.26, §7.9 FR-9.58 (QUIRK Q7/D17), §7.11 FR-11.29/FR-11.30/FR-11.31/FR-11.32/FR-11.34/FR-11.40, AC-11.17/19/20/21/26, §9 NFR-48

**Problem:** The register's only §7.11 row is Q-50 (duplicated manifest). The entire distribution/build quirk class — six separately QUIRK-tagged defects, four of which make the *shipped* binary unusable — is absent. A team that works Phase 0 from the register alone ships a windowed binary with no console, trimmed so plugins vanish, single-file so both Host-linked Commands fail, on a runtime target its own dependencies do not publish. These are routed to OQ-9/OQ-10/OQ-11/OQ-12 in §11.1, but §10 Phase 0 requires answers to only OQ-4, OQ-5, OQ-9, OQ-14 and OQ-17 — so OQ-10, OQ-11 and OQ-12 are gated by nothing at all.

**Required fix:** Add these rows verbatim:
| Q-51 | The shipping Release profile marks the executable as a windowed, non-console binary, so a shipped build gets no terminal: prompt, Status lines, Output and the crash line have nowhere to go. | 7.3, 7.5, 7.6, 7.9, 7.11 | U | **Fix** — the shipped artefact is invisible as configured. See OQ-9. |
| Q-52 | The Release profile retargets an older, Windows-only runtime while every test exercises the current cross-platform one — and that target is unsatisfiable, because both libraries it depends on and the external Command Service publish no build for it. | 7.9, 7.11 | B | **Fix** — one target runtime; what is tested must be what ships. See OQ-10. |
| Q-53 | The Release profile enables whole-program dead-code elimination, which is hostile to a plugin model whose Command types are reached only by runtime reflection: a Plugin that works in a development build may have its Commands silently absent from a shipped build, with no error. | 7.11 | S | **Fix** — trimming without a keep-list silently removes executable extension points. See OQ-11. |
| Q-54 | Single-file packaging leaves the recorded module path empty for Host-linked Commands, and dispatch re-resolves through that path, so `PACKAGE SEARCH` and `PACKAGE INSTALL` fail in the shipped build with `Error executing PACKAGE (see trace for more info)`. | 7.9, 7.11 | U | **Fix** — never re-resolve in-process Commands through a file path. See OQ-12. |
| Q-55 | The private hosted feed is declared with no routing pattern and is therefore never consulted, and the local feed's location is the unexpanded environment token `%NUGET_LOCAL_PACKAGES%`; the product consequently does not restore or build on any host lacking that variable. | 7.2, 7.10, 7.11 | B | **Fix** — this is one of the two reasons the product does not build at the pinned commit. See OQ-2. |
| Q-56 | The processor-architecture axis is inert: every project maps both x64 and x86 to the neutral build, and the shipping target is 64-bit-only regardless. | 7.11 | B | **Fix** silently — delete the axis. |

---

## 3. [CRITICAL] §11.2 — no row for gate placement; body at §7.10 FR-10.2, FR-10.84, AC-10.33

**Problem:** §7.10 FR-10.2 states that all five validation gates "live inside Package Search's own body, not as a shared control", so "the transport gate is therefore absent from every other outbound path in the Package Registry Client — version enquiry, download and dependency resolution are ungated", and AC-10.33 pins that a version enquiry against a plain-`http` endpoint runs with no transport check. The register's supply-chain rows (Q-6, Q-14, Q-15, Q-33, Q-47, Q-48) do not cover this, so the highest-leverage structural fix in the document — gate by construction, not by placement — is not on the Phase 0 decision list.

**Required fix:** Add:
| Q-57 | The HTTPS transport gate exists only inside Package Search's own body; version enquiry, archive download and dependency resolution take an already-built connection and check nothing, so every outbound path but one is ungated. | 7.7, 7.10 | S | **Fix** — gate every outbound path by construction, not by placement. |

---

## 4. [CRITICAL] §7.2 FR-2.37 and AC-2.15 (also §7.2 UC-2.3 E3, §7.10 FR-10.4, §8 DM-3 Lifecycle, §11.2 Q-10) — contradicted by §7.8 FR-8.16

**Problem:** Five locations assert the disproved consequence as fact, and one of them is an executable acceptance criterion a clone would be tested against. §7.2 FR-2.37: "an entry `PACKAGESOURCEURL` with an empty value appears in every subsequent Session Variable dump"; §7.2 AC-2.15: "**and** a Session Variable dump afterwards shows an entry `PACKAGESOURCEURL` with an empty value"; §7.10 FR-10.4: "shall store an empty value under it as a side effect, after which a Session Variable dump lists it"; §8 DM-3: "the first Package Search **permanently** adds an empty `PACKAGESOURCEURL` entry that a subsequent variable dump then lists" — which the same paragraph then contradicts three sentences later with its own write-back rule; §11.2 Q-10 records it as a quirk of "the variable store" and attributes it to "6, 7.2" only. §7.8 FR-8.16 is correct: the child scope is discarded because Package Search is registered as not modifying Session Variables. A clone built to AC-2.15 must either wire write-back for a non-modifying Command or ship a test its faithful implementation fails.

**Required fix:** Correct all five. FR-2.37 and §7.10 FR-10.4: end at "...writes the default back into the Command's per-execution child scope, which is then discarded because Package Search is not registered as environment-modifying; the Session's store is unchanged." AC-2.15: replace the second clause with "**and** a Session Variable dump afterwards shows **no** `PACKAGESOURCEURL` entry — the write landed in the discarded child scope; the write is observable only when a host invokes the Command directly against its own store." §8 DM-3: delete "permanently" and the dump clause, and cross-reference the write-back sentence in the same paragraph. §11.2 Q-10: restate as "a read mutates the Command's child scope (invisible in the Session today, and visible the moment a Command is declared environment-modifying)" and add §7.8 to its § column.

---

## 5. [MAJOR] §11.2 Quirk register (lines 8023–8084), against §7.3 AC-3.18 / FR-3.4, §7.9 AC-9.7 and §7.9 AC-9.17

**Problem:** The register has exactly 50 rows Q-1…Q-50 with no gaps or duplicates, matching its "Fifty distinct quirks were catalogued" claim — but it is not complete. The "How to read this document" preamble states "Every quirk with an observable consequence has an acceptance criterion that pins it, and a keep-or-fix entry in §11", and §10 Phase 0's deliverable is "a signed-off disposition for every entry in the §11.2 quirk register". At least three AC-pinned, user-visible quirks have no register row and no OQ: (a) AC-9.17 / §7.9 local Q12 — a bare Command Group word (`package`) renders `Error executing PACKAGE (see trace for more info)` plus `**Error: Command type name is empty.` instead of listing the group's members; (b) AC-9.7 / §7.9 local Q14, explicitly flagged "newly identified here" — quoting protects a dotted argument at the Prompt but not inside a Pipeline (Q-43 covers dot destruction but not the prompt-vs-pipeline asymmetry); (c) AC-3.18 / FR-3.4 / §7.3 QUIRK-7 — constructing the root Presentation Adapter with no argument list faults with `Value cannot be null. (Parameter 'source')`, which is a different requirement and a different criterion from the child-context case that Q-34 covers (FR-3.50 / AC-3.17). Phase 0 would sign off a register that silently omits them.

**Required fix:** Add three rows — Q-51 (bare group word yields an internal-error pair instead of a member listing; § 7.9; U; Decide), Q-52 (quoting protects a dotted argument at the Prompt but not inside a Pipeline; § 6, 7.9; U; Fix), Q-53 (adapter construction faults when the optional argument list is omitted; § 7.3; D; Fix) — and change §11.2's opening sentence from "Fifty distinct quirks were catalogued" to "Fifty-three distinct quirks were catalogued".

---

## 6. [MAJOR] §11.1 (OQ-1..OQ-25), against §7.9 external-technology note (assembled PRD line 5318 / prd-sections/7.09-F5-package-install.md line 647)

**Problem:** F5's open question 2 — "**What registry would install use?** The sibling search path reads a `PackageSourceUrl` value from the session environment and falls back to `https://api.nuget.org/v3/index.json`. The install routine takes a registry handle from its caller and there is no caller, so no default is observable." — never landed in §11.1. Worse, §7.9 explicitly forwards the reader to it: "A finished install would be expected to honour the same Registry Endpoint setting the search path reads, but nothing in the source establishes that; **see open questions**." There is no such open question. OQ-15 covers the install *directory*, OQ-16 the *dependency* behaviour, OQ-19 which registries *search* can reach — none asks which endpoint a finished install would use. The cross-reference dangles, and the clone team is left with a §7.9 note pointing at nothing while §11.1 reads as a complete register.

**Required fix:** Add an OQ to §11.1 (e.g. OQ-15a, adjacent to OQ-15): "**Which Registry Endpoint would a finished install use?** The install routine takes a pre-built registry connection from a caller that does not exist, so no default is observable; the sibling search path reads the Registry Endpoint Session Variable and falls back to the public index. Why it matters: whether install honours the operator-settable endpoint or hard-wires one is a supply-chain decision. Where we looked: the install routine (`NugetWrapper.cs:111`), both package Commands, the shipping entry point, `NuGet.config` — nothing connects them." Then change the §7.9 text from the bare "see open questions" to a citation of that OQ number.

---

## 7. [MAJOR] §11.1 (OQ-1..OQ-25) and §11.2 Q-14, against §7.4 UC-4.D error flow E2 (assembled PRD line 2973)

**Problem:** F3's open question 4 — "**Does the containment check behave identically for a symbolic link, junction, or UNC path?** … Links and UNC paths remain open — the check canonicalises the textual path but does nothing to resolve links, so an in-bounds link pointing out of bounds is expected to be accepted, and a UNC path has a different location identifier shape whose base-of behaviour was not evaluated" — appears nowhere in §11.1, nowhere in the §11.2 quirk register, and not in §7.4's own thirteen-item "bug-for-bug decisions" list. Its single appearance in the whole 8,200-line document is one error-flow bullet inside UC-4.D. This is the document's most security-relevant undetermined item, and §11.2 Q-14's recommended disposition ("**Fix** — compare the candidate itself against a boundary normalised with a trailing separator") does **not** close it: a team that applies exactly that fix is still open to a symlink escape and has been given no signal that anything remains undecided. Phase 0 (§10) signs off the §11.2 register and named OQs, so this item is never put to the commissioning team at all.

**Required fix:** Add an OQ to §11.1 immediately after the containment material (e.g. OQ-7a): "**Does the Plugin Directory containment check hold against symbolic links, junctions and network-share paths?** Why it matters: the check canonicalises text only and resolves no links, so an in-bounds link pointing out of bounds is expected to be accepted — a direct bypass of the only boundary standing between the Plugin Directory and arbitrary code execution. Where we looked: the reference tag's directory-verification tests, which cover only plainly-inside and plainly-outside absolute paths with an explicitly set boundary; the subject repository's tests, which never touch directory verification. Not exercised by any test in either source; verify empirically for link, junction and network-share candidates." Also add a cross-reference to it from §11.2 Q-14's disposition column and from §7.4's decision list item 1, so the recommended fix is not read as complete.

---

## 8. [MAJOR] §11.2 Q-10 ("Reading the Registry Endpoint setting writes an empty value back into the variable store as a side effect. | 6, 7.2 | D | Fix")

**Problem:** The body does not agree with itself about this row, and the row takes the side the most detailed subsection rejects. §7.8 FR-8.16 says the write lands in "the Command's per-execution child Session Variable scope" which "SHALL be discarded" because Package Search is registered as not modifying Session Variables, and adds "the project's quirk register records the stronger consequence that a later variable dump lists the polluted key; that consequence does not follow". AC-8.27 pins the opposite outcome to Q-10 and says so: "the project quirk register predicts the opposite, and this criterion is the decision point." Meanwhile §7.2 FR-2.37, §7.7 FR-7.69/AC-7.26 and §7.10 FR-10.4/AC-10.5 all assert the dump *does* list the key. A reader building acceptance tests gets two mutually exclusive criteria (AC-8.27 vs AC-7.26/AC-10.5) and no flag in the register that anything is unresolved.

**Required fix:** Restate the row and route it to an open question rather than asserting the disputed consequence:
| Q-10 | Reading the Registry Endpoint setting writes an empty value back under that name as a side effect. Whether it reaches the Session's own store is disputed in the body: §7.2/§7.7/§7.10 say a later variable dump lists `PACKAGESOURCEURL`; §7.8 (FR-8.16, AC-8.27) says the write lands in a discarded child scope because Package Search is registered as not modifying Session Variables, and is observable only on the direct-invocation path. | 6, 7.2, 7.7, 7.8, 7.10 | D | **Fix** — a read should not mutate. Resolve the visibility discrepancy first (new OQ; AC-8.27 vs AC-7.26/AC-10.5 currently contradict). | 
Then add a new OQ in §11.1 for the discrepancy, and reconcile AC-7.26/AC-10.5 with AC-8.27 so only one is asserted as observed.

---

## 9. [MAJOR] §11.2 Q-49 ("Dead code in version enumeration: a second cache scope and a filter are constructed and never used… | 7.7 | D | **Fix** silently.")

**Problem:** Both halves of the row contradict the body. (1) The filter: §7.7 FR-7.23 and AC-7.9 say the unapplied prerelease filter means "prerelease versions **are** present in the result" and that "The clone team must consciously choose to keep this or to apply the filter" — a user-visible behaviour, not silent dead code. (2) The cache scopes: §7.7 FR-7.24 says the scope the request is handed is "never released" and "leaks per call", and AC-7.10 says "A clone that reuses the connection or releases the scope is changing observable resource behaviour and must say so". §9 NFR-35 describes three different lifetimes in one file and says "A clone should treat cache-scope lifetime as an explicit decision rather than copy this. Cross-reference §11 Q-49." Calling all of this "dead code… Fix silently" tells the team no decision is needed, when the body twice says one is.

**Required fix:** Split the row into two, neither silent:
| Q-49 | Version enumeration constructs a prerelease-excluding filter and never applies it, so enumeration returns prerelease versions despite the apparent intent to exclude them. | 7.7, 8 | U | **Decide** — apply the filter or drop it; the result set changes either way (AC-7.9). |
| Q-49a | Cache-scope lifetime is handled three ways in one file: one scope is opened and released, two are created and never released (a leak per call), and one is correctly scoped and then never used because the caller passes the unreleased one instead. A fresh registry connection is also built per call and never reused. | 7.7, 9 | D | **Decide** — releasing the scope or reusing the connection changes observable resource behaviour and must be recorded (AC-7.10, NFR-35). |
Keep the never-read name collection (FR-7.18) as a third, genuinely silent row if a row is wanted for it.

---

## 10. [MAJOR] §11.2 — no rows for §7.1's binder and contract quirks; body at §7.1 FR-1.4, FR-1.14/1.67/1.68, FR-1.21, FR-1.23, FR-1.29, FR-1.44, FR-1.45, FR-1.48, FR-1.63, FR-1.64, FR-1.72, FR-1.77, FR-1.78, FR-1.103, FR-1.105 and their §6 twins GR-5, GR-6, GR-20, GR-21, GR-23, GR-27, GR-28

**Problem:** §7.1 tags 39 requirements QUIRK; the register covers 25 of them. Fourteen distinct §7.1 quirks have no row, including four that change what a typed command line does (`-taken` silently binding `take` and eating the next token; a trailing `-take` with no value crashing the binder; `-VERBOSITY quiet` binding nothing; the conditional-echo Built-in emitting the wrong token) and one security row (the dormant redaction policy). Phase 1 is "the largest and least negotiable phase" and is built from §7.1, so these are exactly the decisions the register exists to force.

**Required fix:** Add:
| Q-58 | Parameter-name matching is unanchored at the end: a token that merely begins with one or two dashes plus the declared name matches, so `-taken` binds a parameter declared `take` and swallows the following token as its value. First match in token order wins. | 6, 7.1, 7.8 | U | **Fix** — anchor the match. |
| Q-59 | A Named Parameter as the final token with no value after it reads past the end of the token list, so the user sees a raw index failure instead of a product message. | 6, 7.1, 7.8, 7.10 | U | **Fix** — diagnose a value-less Named Parameter explicitly. |
| Q-60 | Parameter *names* match case-sensitively and are lower-cased at declaration while parameter *values* do not, so `-VERBOSITY quiet` matches nothing and binds the default (or fails as missing), and a declaration whose name contains upper-case letters is unreachable in the form its author wrote it. | 6, 7.1 | U | **Fix** — match names case-insensitively. |
| Q-61 | Registering a Command by instance records only the Command's kind; the supplied object is discarded and a fresh instance is constructed for every execution. | 7.1 | D | **Keep** — but document it: state placed on a registered object does not survive. |
| Q-62 | The "may change Session Variables" flag is stamped on the Command Group's record, not the member: the first registration of a group decides write-back for every member, and a member joining later cannot change it. | 6, 7.1, 8 | D | **Decide** — per-member write-back is the defensible design. |
| Q-63 | A quoted or escaped pipe still routes the line into the Pipeline machinery (the delimiter search runs before quote handling), and the Pipeline splitter consumes quote characters before re-tokenising each stage — so quoting protects a dotted argument at the top level but not inside a pipeline: `say x \| package install "XCBatch.Core"` emits `Not installing x XCBatch,Core`. | 6, 7.1, 7.9 | U | **Fix** — quoting must behave identically in both positions. |
| Q-64 | A final Pipeline stage emitting more than 10,000 Outputs blocks forever: the final Stage Channel is bounded at 10,000 with a blocking full-buffer policy and has no reader until every stage has completed. | 7.1, 9 | U | **Fix** — this is a hang. |
| Q-65 | A `%NAME%` Session Variable reference is destroyed by tokenization unless double-quoted, and an unresolved reference comes back with doubled delimiters (`SAY "%NOPE%"` renders `%%NOPE%%`); the echo Built-in also prints the names it failed to resolve. | 6, 7.1 | U | **Fix** — the product's own help text tells users to double-quote for this reason. |
| Q-66 | The conditional-echo Built-in echoes the wrong token: it tests every token after the first but appends the token at position 1 once per match, so `REGIF a xa ba` emits `xa xa` rather than `xa ba`. | 7.1 | U | **Fix.** |
| Q-67 | A child variable scope is born already flagged "changed" (seeding runs through the write path that sets the flag) and never records its parent, though the contract requires the lineage field. | 6, 7.1, 8 | D | **Fix** — the change flag is meaningless as it stands. |
| Q-68 | The Command Service's own attribute-driven help generator is unreachable for any Command built on the shared Command base, and a synthesised usage line is written flush left while a declared prototype is indented two spaces. | 7.1 | D | **Fix** silently — but keep the non-empty-wins fallback. |
| Q-69 | The Shell's test stand-ins are not the published contract: they declare members the contract lacks, name the load-Commands argument as a configuration path rather than a binary sub-directory defaulting to `bin`, and read the third argument of a variable read as "raise if absent" rather than "store the fallback" — the opposite of the contract. | 7.1 | D | **Fix** — a reimplementer following the stand-ins builds the wrong load operation and the wrong read semantics. |
| Q-70 | The framework's audit redaction policy is dormant: masking is applied only to tokens shaped `-name=value` and the executor records the raw token list, so a secret typed as a Named Parameter reaches the audit record unmasked. | 7.1, 8 | S | **Fix** — apply the policy to the recorded parameters. |

---

## 11. [MAJOR] §11.2 — no row for verb displacement or for the non-executable group verb; body at §6 GR-53, §7.4 FR-4.9/FR-4.47/AC-4.9 ("decision 7: a supply-chain concern with no diagnostic"), §7.1 FR-1.18, §7.4 FR-4.15/AC-4.8, §7.9 FR-9.34/AC-9.17 ("QUIRK Q12")

**Problem:** Two distinct registration quirks have no row. (1) Registration is last-write-wins by name, so a Plugin declaring a verb already held by a Built-in or Host-linked Command "displaces it with no warning, no trace line and no user-visible notice" (GR-53) — §7.4 lists this as its seventh keep-or-fix decision and calls it a supply-chain concern. Q-15 covers only a Plugin that *fails* to load, not one that succeeds and silently takes over `SAY`. (2) A Command Group's synthesised entry carries no executable identity, so typing `PACKAGE` alone yields `Error executing PACKAGE (see trace for more info)` plus `**Error: Command type name is empty.` with nothing telling the user which members exist. Q-8 covers only that failure text names the group rather than the member; Q-13 covers namespacing and recommends "Keep".

**Required fix:** Add:
| Q-71 | Command registration is last-write-wins by name (with member-merging for Command Groups), so a Plugin that declares a verb already held by a Built-in or Host-linked Command displaces it with no warning, no trace line and no user-visible notice. | 6, 7.4 | S | **Fix** — surface the displacement; silently re-pointing an existing verb at Plugin code is a supply-chain gap. |
| Q-72 | A Command Group's synthesised entry carries no implementation identity, so the bare group verb — or a member word that matches no member — produces `Error executing PACKAGE (see trace for more info)` plus `**Error: Command type name is empty.`, never a member listing or a "missing sub-command" message. | 7.1, 7.4, 7.9 | U | **Fix** — print the member listing for a bare or unmatched group verb. |

---

## 12. [MAJOR] §11.2 — no rows for the product-wide reporting quirks; body at §6 GR-46 and GR-47, §7.6 FR-6.45/FR-6.48/FR-6.49, AC-6.22/AC-6.23/AC-6.25, §7.10 FR-10.24/AC-10.7/AC-10.11, §7.8 FR-8.48/FR-8.49/AC-8.22, §7.2 FR-2.52/FR-2.55/AC-2.6

**Problem:** Four product-wide QUIRK-tagged behaviours have no register row. §6 GR-46 ("The product shall silently correct invalid values rather than reporting them") and GR-47 ("Work the product does not perform… shall be announced as ordinary Output, never as failures") are stated as product-wide requirements with their own keep-or-fix framing, and §7.6 FR-6.49 says registry diagnostics are "discarded by construction" so "a Registry outage and a zero-result search are indistinguishable" — an availability defect Q-7 (zero results print nothing) does not cover. Q-7's disposition "emit an explicit no matches" is unimplementable without also deciding the outage case.

**Required fix:** Add:
| Q-73 | Invalid values are silently corrected rather than reported: a Result limit outside 1–100 is clamped, a search term over 200 characters is truncated to 200, and a blank search term returns empty Output and issues no query — none produces any message. | 6, 7.6, 7.8, 7.10 | U | **Decide** with Q-7 — reporting these is more helpful and less faithful. |
| Q-74 | Work the product does not perform is announced as ordinary Output, never as a failure, and never affects exit status: Package Install answers `Not installing ` plus its comma-joined arguments, and a piped Package Search answers `Unsupported search method for <chunk> (piped)` immediately followed by its comma-joined arguments with no separator, so they run into the closing parenthesis. | 6, 7.2, 7.6, 7.8 | U | **Fix** — route refusals to the failure channel and add the missing separator. |
| Q-75 | Registry-side diagnostics are discarded by construction: the Package Registry Client accepts a diagnostic sink at every entry point and every call site in the product passes the discard sink, so a Registry outage, a feed error and a genuine zero-result search are indistinguishable to the user. | 7.6, 7.7 | S | **Fix** — a supply-chain client that cannot report transport failure has no failure signal at all. |
| Q-76 | The No Plugins Available guidance travels on the Output channel rather than the Status channel, so a Shell started with Status Visibility off suppresses `Loading Commands` but still prints the guidance, and the guidance renders in Output colours. | 7.2, 7.3 | D | **Fix** — put narration on one channel. |

---

## 13. [MAJOR] §11.2 — Q-46/Q-48 do not cover the client-side install routine; body at §7.7 FR-7.3/FR-7.54/FR-7.56, AC-7.20/AC-7.21, §7.9 FR-9.51 (QUIRK D18), §11.1 OQ-15

**Problem:** Q-46 covers the Package Install *Command* being inert and Q-48 covers download verification, but the half-built client install *routine* has no row: §7.7 FR-7.54 says it "stops after creating the per-Plugin directory" so a "successful" install produces one archive plus one empty directory, and FR-7.56 says the shape it creates "does not match the layout the Plugin Scanner searches", so "even a completed extraction would not be discovered at the next startup" (AC-7.21, OQ-15). A team that resolves Q-46 by finishing the Command still ships a feature that installs nothing loadable. §7.7 FR-7.3 (only version enumeration builds its own connection; every other operation demands a pre-built one) also has no row.

**Required fix:** Add:
| Q-77 | The client's install routine stops after creating the per-version directory — nothing is extracted and no dependency is resolved — and the shape it creates (`{target}/{id}/{version}/`) is not the layout the Plugin Scanner searches (`<PluginDirectory>/*/bin/*.dll`), so even a completed extraction would not be discovered at the next startup. | 7.7, 7.9 | U | **Decide** with Q-46 — settle the install layout against the scan layout before writing extraction. See OQ-15. |
| Q-78 | The Registry Client's operations disagree about connections: version enumeration takes an endpoint as plain text and builds its own, archive-identity read takes none, and every other operation requires an already-built connection supplied by the caller. | 7.7 | D | **Fix** — one consistent connection contract. |
| Q-79 | The Registry Client carries a second, independent copy of the Result-limit default (`20`) and the prerelease default (off), and hard-codes the query start offset to `0` with no way to change it. | 7.2, 7.7 | D | **Fix** silently — one source of truth for the defaults. |

---

## 14. [MAJOR] §11.2 — no rows for FR-6.31 or AC-8.15; body at §7.6 FR-6.31, §7.8 FR-8.25/AC-8.15 ("QUIRK pin")

**Problem:** Two QUIRK-tagged behaviours with observable consequences have no row. §7.6 FR-6.31: "A blank or whitespace-only configured Plugin Directory is a fatal startup failure, not the benign branch" — a Shell Operator who clears the setting to disable plugins kills startup instead. Q-11 covers only the present-but-empty *directory*, not a blank setting *value*. §7.8 AC-8.15 pins that the Result limit bounds records, not output lines, so `-take 1` at the full detail level produces six lines; §7.8 lists this as its tenth keep-or-fix decision.

**Required fix:** Add:
| Q-80 | A blank or whitespace-only configured Plugin Directory is a fatal startup failure rather than the benign No Plugins Available branch, because registration rejects it by raising and the raise is not the benign type. Reachable only for an Operator who overrides the setting. | 7.6 | U | **Fix** — an emptied setting should mean "no plugins", not "do not start". |
| Q-81 | The Result limit bounds records, not output lines: `-take 1` at the full detail level produces six lines. Whether the limit was meant as a record count or an output-size control is not determinable from the repository. | 7.8 | D | **Decide** — record count is the defensible reading; state it. |

---

## 15. [MAJOR] §7.8 FR-8.16 ("the discrepancy is routed to open questions") vs §11.1

**Problem:** FR-8.16 defers the conflict it correctly identifies: "the project's quirk register records the stronger consequence that a later variable dump lists the polluted key; that consequence does not follow from the observed discard rule for a non-modifying Command, and the discrepancy is routed to open questions." §11.1 contains OQ-1 through OQ-25 and none of them is this question; §11.2 Q-10 instead records the disproved version as settled fact. The routing promised in the deliverable was never performed, so the only place the conflict is flagged is a parenthetical inside one feature subsection.

**Required fix:** Resolve it rather than route it — the framework evidence settles it (see the two findings above). Delete the "*Unresolved:* … routed to open questions" sentence from FR-8.16, keep the substantive statement, and correct §11.2 Q-10. If the team prefers to defer, add an actual OQ-26 to §11.1 naming both readings and the framework line (`CommandExecutor.cs` write-back guard) that decides it, and make FR-8.16 cite it by number.

---

## 16. [MAJOR] §6 GR-41 (contradicted by §7.2 FR-2.17, §7.4 FR-4.8, §7.5 FR-5.17, §7.6 UC-6.4/AC-6.26, §7.10 FR-10.60, §11.2 Q-17)

**Problem:** GR-41 states the startup-failure rule as "Any failure of the three guarded startup steps **other than No Plugins Available** shall be replaced by a Startup Load Failure whose summary is the fixed, cause-independent literal `Unable to load commands.` — **identical in both Session entry points**". Read as written, the No-Plugins-Available exemption holds in both entry points. Six other locations say the opposite: the asynchronous/background entry point has no tolerant branch and wraps No Plugins Available as fatal too (`Loop.cs:82-85` has only the generic catch). §6 also never records the two unrelated "nothing found" signals or which one is fatal, although §7.6's external-technology note calls collapsing them "the single most consequential substitution error available here" and §7.5's calls the two distinguishable signals "critical". §6 is the product-wide normative section a clone team will build the failure contract from.

**Required fix:** Split the clause: "In the blocking entry point, any failure other than No Plugins Available is replaced by a Startup Load Failure …; in the background entry point there is no tolerant branch, so **every** failure including No Plugins Available is wrapped. The summary literal is identical in both." Add a new §6.5 rule stating that the Command Service raises two unrelated sibling conditions — "no verified Plugin Directory" (caught, becomes the guidance line) and "Plugin Directory holds no plugin binaries" (uncaught, fatal, exit 1) — and that collapsing them inverts the first-run experience, cross-referencing §7.4 FR-4.42 and §7.6 FR-6.32.

---

## 17. [MINOR] §3.7 Actor → feature map, "Handed to §11 (open questions)" paragraph (lines 651–658) versus §11.1 (lines 7990–8021)

**Problem:** §3.7 states that five questions are "handed to §11 (open questions)". Two of them never arrive. There is no open question about adding an audit trail — the string "audit" does not occur anywhere in §11, even though NG-9, DM-18, NFR-54 and §9.7 all rest on the declined audit hook — and there is no open question about introducing a non-interactive/batch mode (neither "batch" nor "non-interactive" occurs in §11; Q-22 covers only the redirected-input hang, not the mode decision). The register runs OQ-1…OQ-25 with no gaps or duplicates, so these are omissions rather than numbering errors, and a reader who follows §3.7's pointer finds nothing.

**Required fix:** Add OQ-26 ("Should the clone install an audit sink, given that the required capability offers a fully specified hook the product leaves at its no-op default, with a redaction policy that is dormant because the execution path records raw tokens?", citing NG-9, DM-18, NFR-54) and OQ-27 ("Should a non-interactive/batch mode exist, given §3.5 shows none and redirected input never terminates?", citing Q-22, NG-3); or delete those two clauses from §3.7.

---

## 18. [MINOR] §11.1 / §11.2 Q-7, against §7.6 FR-6.49 and decision-list item 13 (assembled PRD lines 3748, 3852)

**Problem:** F9's open question 9 — "**Should registry-side diagnostics be surfaced?** Every registry call passes a discard sink, so a feed outage and an empty result look identical to the operator" — is documented in the body (FR-6.49, AC-6.25, §7.6 decision 13) but reaches neither §11.1 nor the §11.2 quirk register, which is the table Phase 0 signs off. It is not a resolved question: the disposition is still open. The concrete harm is that it interacts with a register entry that *is* there: §11.2 Q-7 recommends "**Fix** — emit an explicit 'no matches'" for zero-result searches. A team that implements Q-7 without knowing about FR-6.49 will print "No matches" when the registry is unreachable — turning a silent failure into an actively false statement.

**Required fix:** Add a row to the §11.2 quirk register: "Q-7a | Registry-side diagnostics are discarded at every call site, so a feed outage and an empty result are indistinguishable. | 7.6, 7.7 | U | **Fix** — surface registry diagnostics, or Q-7's 'no matches' message will lie during an outage." and add "see Q-7a" to Q-7's disposition cell so the two decisions are taken together.

---

## 19. [MINOR] §11.1 OQ-3 ("Where we looked" column) and §11.3 assumption 1, against §11.2 Q-10

**Problem:** F8's "Could not determine" entry flags a specific consequence that was dropped: "I could not determine whether 2.1.0's contract genuinely carried the misspelled member and the different third-argument meaning, or whether the test double is simply over-specified. **This matters for R28 (does a missing-key read really write an empty entry back?)**". OQ-3 records the divergence itself ("a third read argument whose meaning differs ('required, throw' vs 'store the default')") but its load-bearing list names only the always-present flag key (OQ-4), the case-insensitive allow-list (OQ-5) and the empty-argument short circuit, and §11.3 assumption 1 tells the reader to re-verify only OQ-4, OQ-5 and OQ-8. Meanwhile §11.2 Q-10 states the write-back as settled fact with a disposition ("Reading the Registry Endpoint setting writes an empty value back into the variable store as a side effect. **Fix** — a read should not mutate.") and carries no "See OQ-3" pointer, unlike Q-1, Q-2, Q-6, Q-11 and their siblings. AC-2.15 then pins the write-back as an executable acceptance criterion. If the pinned 2.1.0 contract read that third argument as "required, throw" — which the repository's own test double asserts — the quirk does not exist and the acceptance criterion is wrong.

**Required fix:** Extend OQ-3's "Why it matters" column to name the mutating read-with-default alongside the other load-bearing items ("…the empty-argument short circuit, and the read-with-default write-back behind GR-26 / FR-2.37 / Q-10 / AC-2.15"); add OQ-3 to §11.3 assumption 1's re-verify list ("Re-verify OQ-4, OQ-5, OQ-8 and the read-with-default semantics behind Q-10"); and add "See OQ-3" to Q-10's disposition cell.

---

## 20. [MINOR] §11.2 Q-50 ("The dependency-version manifest is duplicated byte-for-byte at two levels…") and §7.7 source-notes line ("duplicated at `src/Directory.Packages.props`… byte-identi[cal]")

**Problem:** "byte-for-byte" is factually wrong and contradicts the body's most precise statement. §7.11 FR-11.43 records the two copies as "identical line for line but **not** byte-identical — the root copy ends with a line terminator (813 bytes) and the source-folder copy does not (811 bytes), so a byte-level comparison reports them as different", and AC-11.25 pins exactly those byte counts. A team that implements Q-50 by adding a byte-comparison drift check gets a permanently failing check on day one.

**Required fix:** Rewrite the row as:
| Q-50 | The dependency-version manifest is duplicated at two levels with identical content but different bytes — the root copy ends with a line terminator (813 bytes), the source-folder copy does not (811 bytes) — and the two are free to drift. | 7.2, 7.11 | B | **Fix** — keep one; a byte-level comparison already reports them as different (AC-11.25). |
Also correct the "byte-identical" wording in §7.7's External-technology note to match FR-11.43.

---

## 21. [MINOR] §11.2 Q-26 ("The Plugin Directory resolves against the process working directory, not the executable's directory… | 7.4")

**Problem:** The § pointer sends the reader to a subsection that does not document the quirk. §7.4 FR-4.18 states only that the default is "resolved against the process working directory"; it never contrasts that with the executable's directory and never states the launch-location consequence. The subsection that actually documents this quirk is §7.11 — FR-11.9 ("there SHALL be no override. Launching the same artifact from a different working directory therefore changes where Plugins are sought, with no diagnostic") and AC-11.10, which pins it.

**Required fix:** Change the § column of Q-26 from `7.4` to `7.4, 7.11`, and cite FR-11.9/AC-11.10 as the pinning criterion.

---

## 22. [MINOR] §11.2 Q-21 ("…| D | **Keep** — correct and intended behaviour; document it.") and Q-13 ("…| **Keep** — deliberate namespacing…")

**Problem:** Both dispositions assert intent the body never establishes, and both understate severity. For Q-21, §7.5 AC-5.14 says "QUIRK: exit words shadow Command names; keep or fix deliberately" — an open keep-or-fix, not settled intent — and the consequence (a Plugin Command named `END`, `EXIT` or `BYEE` is permanently unreachable) is user-visible, not `D`. For Q-13, §7.4 AC-4.8 is likewise tagged "QUIRK — decide keep or fix", and §7.9 FR-9.34 treats the group verb's behaviour as an open decision. Presenting these as settled removes them from Phase 0's agenda on the strength of a claim the body does not support.

**Required fix:** Change Q-21's severity to `U` and its disposition to "**Decide** — the loop tests exit words before dispatch, so a Plugin Command named `END`, `EXIT` or `BYEE` is unreachable; keep or fix deliberately (AC-5.14)." Change Q-13's disposition to "**Decide** — namespacing is defensible, but the body records it as an open keep-or-fix (AC-4.8); fix the guidance text either way (Q-12)."

---

## 23. [MINOR] §11.2 Q-34 and Q-45; body at §7.3 FR-3.4 (QUIRK-7) / AC-3.18, and §7.1 FR-1.60

**Problem:** Two rows are each narrower than the quirk the body documents, leaving a real case with no decision attached. (1) Q-34 covers only child creation without an argument list; §7.3 FR-3.4/AC-3.18 tag a separate QUIRK-7 for constructing the *root* Presentation Adapter with no argument list, which fails with the same `Value cannot be null. (Parameter 'source')` even though the argument is declared optional. (2) Q-45 covers only help on a Command Group member; §7.1 FR-1.60 states the broader defect — `HELP <command>` ignores its argument entirely and always prints the full listing, so there is no path from the Prompt to *any* single Command's help, grouped or not.

**Required fix:** Broaden both rows in place rather than adding new ones. Q-34: "Creating an Interaction Context without an argument list faults — at child creation *and* at root construction — although the contract declares that form legal. | 7.3 | D | **Fix** — default to an empty list." Q-45: "`HELP <command>` ignores its argument and always prints the full listing, and requesting help on a member of a Command Group prints the group listing — so no invocation from the Prompt prints any single Command's own parameter help. | 7.1, 7.9 | U | **Fix.** See OQ-14's neighbourhood."

---

## 24. [MINOR] §11.2 Q-48; body at §9 NFR-12

**Problem:** §9 NFR-12 is QUIRK-tagged ("Two concurrent downloads to the same target path corrupt each other": create-or-truncate semantics with no lock and nothing serialising two downloads of the same file) and explicitly notes it is "a defect a clone finishing that feature will inherit". Q-48 records unconditional overwrite but not the concurrency case, so a team that resolves Q-48 by adding verification still ships the race.

**Required fix:** Extend Q-48's quirk text to "…Downloads overwrite their target unconditionally with create-or-truncate semantics and no lock, so two concurrent downloads to the same path corrupt each other; package identity is taken from inside the downloaded archive, and nothing verifies a signature, checksum or publisher before the code is loaded." and change its § column to `7.7, 7.10, 9`.

---

## 25. [MINOR] §7.9 FR-9.31 and AC-9.7 vs §11.2 preamble

**Problem:** §11.2 opens "Fifty distinct quirks were catalogued. Each is documented as observed in its feature subsection and pinned by an acceptance criterion where it has an observable consequence," and §10 Phase 0 makes that table the complete disposition list. §7.9 FR-9.31 documents and AC-9.7 pins a quirk that is not in it: the Pipeline splitter consumes quote characters, so quoting protects a dotted argument at the top level but not inside a Pipeline (`say x | package install "XCBatch.Core"` yields `Not installing x XCBatch,Core`). §11.2's Q-43 covers only the top-level tokenizer behaviour. §7.9 says so itself ("not in the shared quirk register") but nothing was added.

**Required fix:** Add a row to §11.2: "Q-51 | Quoting protects a dotted argument at the top level but not inside a Pipeline — the splitter consumes the quotes and each stage is re-tokenised. | 7.9 | U | **Fix** with Q-43." Update the §11.2 preamble and §1's "50 are catalogued" to fifty-one, and point FR-9.31/AC-9.7 at the new id.

---

