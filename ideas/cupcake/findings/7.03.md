# Verification findings for 7.03-F2-console-presentation.md

15 finding(s). Apply every one.

## 1. [CRITICAL] §11.2 — no row for the shipping Release profile; body at §7.3 FR-3.77 (QUIRK-10), §7.5 FR-5.55, §7.6 FR-6.26, §7.9 FR-9.58 (QUIRK Q7/D17), §7.11 FR-11.29/FR-11.30/FR-11.31/FR-11.32/FR-11.34/FR-11.40, AC-11.17/19/20/21/26, §9 NFR-48

**Problem:** The register's only §7.11 row is Q-50 (duplicated manifest). The entire distribution/build quirk class — six separately QUIRK-tagged defects, four of which make the *shipped* binary unusable — is absent. A team that works Phase 0 from the register alone ships a windowed binary with no console, trimmed so plugins vanish, single-file so both Host-linked Commands fail, on a runtime target its own dependencies do not publish. These are routed to OQ-9/OQ-10/OQ-11/OQ-12 in §11.1, but §10 Phase 0 requires answers to only OQ-4, OQ-5, OQ-9, OQ-14 and OQ-17 — so OQ-10, OQ-11 and OQ-12 are gated by nothing at all.

**Required fix:** Add these rows verbatim:
| Q-51 | The shipping Release profile marks the executable as a windowed, non-console binary, so a shipped build gets no terminal: prompt, Status lines, Output and the crash line have nowhere to go. | 7.3, 7.5, 7.6, 7.9, 7.11 | U | **Fix** — the shipped artefact is invisible as configured. See OQ-9. |
| Q-52 | The Release profile retargets an older, Windows-only runtime while every test exercises the current cross-platform one — and that target is unsatisfiable, because both libraries it depends on and the external Command Service publish no build for it. | 7.9, 7.11 | B | **Fix** — one target runtime; what is tested must be what ships. See OQ-10. |
| Q-53 | The Release profile enables whole-program dead-code elimination, which is hostile to a plugin model whose Command types are reached only by runtime reflection: a Plugin that works in a development build may have its Commands silently absent from a shipped build, with no error. | 7.11 | S | **Fix** — trimming without a keep-list silently removes executable extension points. See OQ-11. |
| Q-54 | Single-file packaging leaves the recorded module path empty for Host-linked Commands, and dispatch re-resolves through that path, so `PACKAGE SEARCH` and `PACKAGE INSTALL` fail in the shipped build with `Error executing PACKAGE (see trace for more info)`. | 7.9, 7.11 | U | **Fix** — never re-resolve in-process Commands through a file path. See OQ-12. |
| Q-55 | The private hosted feed is declared with no routing pattern and is therefore never consulted, and the local feed's location is the unexpanded environment token `%NUGET_LOCAL_PACKAGES%`; the product consequently does not restore or build on any host lacking that variable. | 7.2, 7.10, 7.11 | B | **Fix** — this is one of the two reasons the product does not build at the pinned commit. See OQ-2. |
| Q-56 | The processor-architecture axis is inert: every project maps both x64 and x86 to the neutral build, and the shipping target is 64-bit-only regardless. | 7.11 | B | **Fix** silently — delete the axis. |

---

## 2. [MAJOR] §7.3 Console Presentation & Interaction — FR-3.4 (line 2581), FR-3.15 (2598), FR-3.77 (2694), AC-3.5 (2717), AC-3.10 (2722), AC-3.16 (2728), AC-3.19 (2731), and the numbered list at 2775–2790

**Problem:** §7.3 introduces a second, independent local quirk numbering, "QUIRK-1" … "QUIRK-16", which likewise collides with §11.2's global Q-1…Q-50 and is never declared as subsection-scoped. Concretely: §7.3's "QUIRK-8" (the Prompt never restores terminal styling, AC-3.5) is register **Q-31**, while register Q-8 is "failure text names the Command Group, not the member the user typed" (a §7.8 quirk); §7.3's "QUIRK-3" (progress ratio rendered as a percentage) is register **Q-29**, while register Q-3 is "the detail-level presence test is always true". Two different local schemes plus one global scheme, all rendered with near-identical tokens (`QUIRK-8`, `QUIRK Q8`, `Q-8`), make the quirk cross-references in §7.3 and §7.9 actively misleading.

**Required fix:** Replace every "QUIRK-n" token in §7.3 with its §11.2 register identifier: QUIRK-1→Q-27, QUIRK-2→Q-32, QUIRK-3→Q-29, QUIRK-4→Q-28, QUIRK-5→Q-22, QUIRK-6→Q-33, QUIRK-8 and QUIRK-16→Q-31, QUIRK-9→Q-35, QUIRK-11→Q-34, QUIRK-13→Q-37, QUIRK-14→Q-36, QUIRK-15→Q-20; QUIRK-10 has no register row — cite OQ-9; QUIRK-12 is the vestigial second executable — cite §2.3 X-1; QUIRK-7 needs the new register row described below.

---

## 3. [MAJOR] §11.2 Quirk register (lines 8023–8084), against §7.3 AC-3.18 / FR-3.4, §7.9 AC-9.7 and §7.9 AC-9.17

**Problem:** The register has exactly 50 rows Q-1…Q-50 with no gaps or duplicates, matching its "Fifty distinct quirks were catalogued" claim — but it is not complete. The "How to read this document" preamble states "Every quirk with an observable consequence has an acceptance criterion that pins it, and a keep-or-fix entry in §11", and §10 Phase 0's deliverable is "a signed-off disposition for every entry in the §11.2 quirk register". At least three AC-pinned, user-visible quirks have no register row and no OQ: (a) AC-9.17 / §7.9 local Q12 — a bare Command Group word (`package`) renders `Error executing PACKAGE (see trace for more info)` plus `**Error: Command type name is empty.` instead of listing the group's members; (b) AC-9.7 / §7.9 local Q14, explicitly flagged "newly identified here" — quoting protects a dotted argument at the Prompt but not inside a Pipeline (Q-43 covers dot destruction but not the prompt-vs-pipeline asymmetry); (c) AC-3.18 / FR-3.4 / §7.3 QUIRK-7 — constructing the root Presentation Adapter with no argument list faults with `Value cannot be null. (Parameter 'source')`, which is a different requirement and a different criterion from the child-context case that Q-34 covers (FR-3.50 / AC-3.17). Phase 0 would sign off a register that silently omits them.

**Required fix:** Add three rows — Q-51 (bare group word yields an internal-error pair instead of a member listing; § 7.9; U; Decide), Q-52 (quoting protects a dotted argument at the Prompt but not inside a Pipeline; § 6, 7.9; U; Fix), Q-53 (adapter construction faults when the optional argument list is omitted; § 7.3; D; Fix) — and change §11.2's opening sentence from "Fifty distinct quirks were catalogued" to "Fifty-three distinct quirks were catalogued".

---

## 4. [MAJOR] §7.2 US-2.9, US-2.10, UC-2.1 E5, UC-2.5 E3, UC-2.6 E4; §7.3 US-3.8, US-3.9, US-3.12, US-3.13; §7.5 US-5.4; §7.6 US-6.2, US-6.9; §7.10 US-10.12

**Problem:** The "see FR-x.y" pointer attached to an aspirational or caveated user story lands on an unrelated requirement in at least thirteen places — precisely the pointer a reader follows to check whether a story's caveat is evidenced. Verified: US-2.9 cites FR-2.13 (Plugin sub-folder layout) for the dead install gate, which is FR-2.15. US-2.10 cites FR-2.16 (Command Service read-only) for the two-start-mode divergence, which is FR-2.17. UC-2.1 E5 cites FR-2.29 (fire-and-forget Status write) for the missing-parameter-list construction failure, which is FR-2.31. UC-2.5 E3 cites FR-2.24 (Status Visibility gates Status lines) for "Diagnostic trace never reaches the screen", which is FR-2.25. UC-2.6 E4 cites FR-2.55 (Output-versus-Status channel choice) for the build break, which is FR-2.66. US-3.8 cites FR-3.29 (no echo suppression/history) for the progress-ratio quirk, which is FR-3.30. US-3.9 cites FR-3.37 (nobody calls Progress) for the shadowed visibility flag, which is FR-3.40, and for the unused trace-log facility, which is FR-3.42. US-3.12 cites FR-3.42 for non-propagation to children, which is FR-3.49. US-3.13 cites FR-3.13/FR-3.42 for suppressed error reporting and non-inheritance, which are FR-3.16 and FR-3.49. US-5.4 cites FR-5.26 (whole-line exit test) for the unresolvable guidance text, which is FR-5.49. US-6.2 cites FR-6.34/FR-6.36 (per-Plugin skip; unknown-command text) for the guidance quirks, which are FR-6.40 and FR-6.42. US-6.9 cites FR-6.28 (end-of-input empty string) for per-Plugin skipping, which is FR-6.34. US-10.12 cites FR-10.13 (the `take` default) for tokenization defeating a typed endpoint, which is FR-10.11.

**Required fix:** Repoint each citation to the requirement that actually carries the claim: US-2.9→FR-2.15; US-2.10→FR-2.17; UC-2.1 E5→FR-2.31; UC-2.5 E3→FR-2.25; UC-2.6 E4→FR-2.66; US-3.8→FR-3.30; US-3.9→FR-3.40 and FR-3.42; US-3.12→FR-3.49; US-3.13→FR-3.16 and FR-3.49; US-5.4→FR-5.49; US-6.2→FR-6.40 and FR-6.42; US-6.9→FR-6.34; US-10.12→FR-10.11. The offsets are not uniform (2, 1, 2, 1, 11, 1, 3, 7, 23, 6, 2), so each must be checked individually rather than shifted en bloc, and every remaining FR/AC citation inside the §7 story and use-case blocks should be re-verified in the same pass.

---

## 5. [MAJOR] §7.3 US-3.8, US-3.9, US-3.12, US-3.13 (cross-references)

**Problem:** Four user stories point at requirements that say something unrelated. US-3.8 cites FR-3.29 for the total÷step ratio, but FR-3.29 is "no input echo suppression, no history…" (the ratio is FR-3.30). US-3.9 cites FR-3.37 for the permanently-off trace flag, but FR-3.37 is "no caller invokes the Progress operation" (the shadowed flag is FR-3.40). US-3.12 cites FR-3.42 for non-propagation to children, but FR-3.42 is the unused trace-log-file facility (non-propagation is FR-3.49). US-3.13 cites "FR-3.13, FR-3.42" for suppression of the Command Service's error reporting and non-inheritance, but FR-3.13 is the visible-Status painting rule (suppression is FR-3.14 and the error text is FR-3.16; non-inheritance is FR-3.49). A clone team following these pointers lands on unrelated requirements for exactly the four quirks the section says must be explicit keep-or-fix decisions.

**Required fix:** Repoint: US-3.8 → FR-3.30; US-3.9 → FR-3.40; US-3.12 → FR-3.49; US-3.13 → FR-3.14, FR-3.16 and FR-3.49.

---

## 6. [MINOR] §7.3 Source notes (lines 2598, 2619, 2645, 2657, 2680, 2694, 2791, 2792) and §7.9 Source notes (lines 5511, 5515, 5525, 5527)

**Problem:** Twelve requirement and note sites cite bare identifiers from the orchestrator's working register — "orchestrator register: D6", "D8", "D9", "D10", "D16", "orchestrator register D21", and in §7.9 "Q5 / D14", "Q10 / D8", "Q7 / D17", "Q6, D18, D19". No D-n identifier is defined anywhere in the PRD; they live only in `workspace/orchestrator-notes.md`, which §12.6 explicitly classifies as a working artefact "retained under `workspace/` for audit; not part of the deliverable". A reader holding only the PRD cannot resolve any of them.

**Required fix:** Delete the D-n citations, or replace each with the in-document anchor that carries the same fact — e.g. §7.3 FR-3.66's "(orchestrator register: D6)" becomes "(see Q-12)", FR-3.77's "(orchestrator register: D16)" becomes "(see OQ-9)", and §7.9's "Q7 / D17" becomes "OQ-12".

---

## 7. [MINOR] §7.1 US-1.13, US-1.14; §7.2 US-2.9; §7.3 US-3.8, US-3.12; §7.6 US-6.5; §7.9 US-9.5, US-9.6; §7.10 US-10.15; §7.11 US-11.9, US-11.10

**Problem:** Eleven user stories across seven subsections stop after the capability clause and never state a benefit — the "so that …" half of the required template is absent. Eight of the eleven are exactly the unfinished-intention or partly-unfinished stories (US-1.14, US-2.9, US-3.8, US-3.12, US-9.5, US-9.6, US-10.15, US-11.9, US-11.10), where the missing benefit is the one piece of information that tells the clone team whether an unimplemented capability is worth building or should be dropped along with the dead setting behind it. US-9.5 and US-2.9, for instance, both describe the never-read install gate, and neither says what an Operator would gain by having it work — which is the open question §7.2 bug-for-bug item 7 and §7.5 item 1 ask the team to answer.

**Required fix:** Complete each story with its benefit clause, e.g. US-1.14 → "…so that a hosted Session can offer the Shell without offering a route to install unreviewed code"; US-9.5 → same benefit, worded from the Operator's side; US-3.8 → "…so that a long-running Command can show progress rather than looking hung"; US-3.12 → "…so that the Shell's output matches the host application's visual identity"; US-6.5 → "…so that a script wrapping the Shell can distinguish a deliberate exit from a failure"; US-11.9 → "…so that a user can run the Shell without installing a runtime first"; US-11.10 → "…so that a Plugin author does not have to test against two different builds". Where no benefit can be evidenced from the source, say so explicitly rather than omitting the clause.

---

## 8. [MINOR] §11.2 Q-34 and Q-45; body at §7.3 FR-3.4 (QUIRK-7) / AC-3.18, and §7.1 FR-1.60

**Problem:** Two rows are each narrower than the quirk the body documents, leaving a real case with no decision attached. (1) Q-34 covers only child creation without an argument list; §7.3 FR-3.4/AC-3.18 tag a separate QUIRK-7 for constructing the *root* Presentation Adapter with no argument list, which fails with the same `Value cannot be null. (Parameter 'source')` even though the argument is declared optional. (2) Q-45 covers only help on a Command Group member; §7.1 FR-1.60 states the broader defect — `HELP <command>` ignores its argument entirely and always prints the full listing, so there is no path from the Prompt to *any* single Command's help, grouped or not.

**Required fix:** Broaden both rows in place rather than adding new ones. Q-34: "Creating an Interaction Context without an argument list faults — at child creation *and* at root construction — although the contract declares that form legal. | 7.3 | D | **Fix** — default to an empty list." Q-45: "`HELP <command>` ignores its argument and always prints the full listing, and requesting help on a member of a Command Group prints the group listing — so no invocation from the Prompt prints any single Command's own parameter help. | 7.1, 7.9 | U | **Fix.** See OQ-14's neighbourhood."

---

## 9. [MINOR] §7.3 FR-3.15, FR-3.30, FR-3.40, FR-3.49, FR-3.66, FR-3.77 and §7.9 FR-9.9, FR-9.35, FR-9.39, FR-9.58

**Problem:** These requirements cite identifiers from a register the reader does not have: "(orchestrator register: D8)", "(orchestrator register: D9)", "(orchestrator register: D10)", "(orchestrator register: D16)", "(orchestrator register: consequence of D8's sibling defect class)", and "QUIRK Q1 / D6", "QUIRK Q10 / D8", "QUIRK Q5 / D14", "QUIRK Q7 / D17". The D-numbers are defined only in `workspace/orchestrator-notes.md`, which §12.6 explicitly lists as "Retained under `workspace/` for audit; **not part of the deliverable**". No D-identifier is defined anywhere in the PRD.

**Required fix:** Replace every `D<n>` citation with the §11.2 register id for the same quirk (D6→Q-12, D8→Q-27, D9→Q-28, D10→Q-29, D14→Q-24, D16→OQ-9/OQ-10/OQ-11, D17→OQ-12), or drop the parenthetical entirely — the §7.3 `QUIRK-n` label and the FR text already identify the quirk.

---

## 10. [MINOR] §7.3 UC-3.4 alternate flow A2

**Problem:** A2 states "a grandchild of the root is `Cupcake Console ContextChildChild`, and Progress text from it reads e.g. `Cupcake Console ContextChild progress 4%`". The quoted Progress literal is the *child's* name, not the grandchild's — `ConsoleContext.cs:40` appends one `Child` per level and `:82` renders `this.Name`, so a grandchild renders `Cupcake Console ContextChildChild progress 4%`. The example contradicts the sentence it is attached to.

**Required fix:** Change the example in A2 to `Cupcake Console ContextChildChild progress 4%` (FR-3.44's own example, which says "a nested context", is fine as-is).

---

## 11. [MINOR] §7.3 FR-3.8

**Problem:** FR-3.8 claims exhaustiveness: "the only occurrences are the declarations at `:20,:23-24,:26-27,:29-30,:31` and their reads inside the painting operations at `:55-56,:68-69,:82,:98-99`". The read of `Verbose` at `ConsoleContext.cs:92` (`if (!Verbose)`) is missing from the read list even though its declaration at `:31` is included, so the stated enumeration is incomplete for a requirement whose whole force is "nothing else touches these members".

**Required fix:** Add `:92` to the list of reads ("…at `:55-56`, `:68-69`, `:82`, `:92`, `:98-99`").

---

## 12. [MINOR] §7.3 FR-3.71 (QUIRK-9) and UC-3.5 error flow E1

**Problem:** FR-3.71 calls painting "a three-step, non-atomic sequence — set foreground, set background, write, reset" and E1 repeats "three-step colour-write-reset sequences", but four operations are enumerated and FR-3.73 states the opposite ("Each painted line issues four terminal operations (two colour sets, one write, one reset)"), which matches `ConsoleContext.cs:55-58` and `:98-101`. Only the Prompt path (`:68-70`) is three operations.

**Required fix:** Say "four-step" in FR-3.71 and UC-3.5/E1 (set foreground, set background, write, reset), noting the Prompt path is the three-step variant with no reset.

---

## 13. [MINOR] §7.3 FR-3.39

**Problem:** FR-3.39 asserts as fact that total `100` / step `10` is "the single input pair on which the shipped formula and the documented formula coincide". The two formulas (total÷step vs step÷total×100) agree for every pair where total = 10 × step — e.g. total 50 / step 5 gives 10 under both. The claim is true only of the rows in FR-3.30's table, not of the input space.

**Required fix:** Reword to "one of the input pairs on which the two formulas coincide (they agree whenever total = 10 × step), and the only such pair in the table above — which is why the defect is not caught".

---

## 14. [MINOR] §7.3 UC-3.2 Preconditions

**Problem:** The precondition reads "The Lit Shell host has constructed the root Presentation Adapter with the name `Cupcake Console Context` and an empty argument list". The host (`src/Xcaciv.Cupcake.Lit/Program.cs:9-12`) constructs no Interaction Context at all; it calls `RunWithDefaults()`, and the Session constructs the adapter at `src/Xcaciv.Cupcake.Core/Loop.cs:110`. §7.3 FR-3.2 and §7.5 FR-5.20 both attribute it correctly, so this precondition contradicts them.

**Required fix:** Reword to "The Session's defaults start has constructed the root Presentation Adapter … (`Loop.cs:110`), invoked by the Lit Shell host (`Program.cs:12`)".

---

## 15. [MINOR] §7.3 FR-3.57

**Problem:** FR-3.57 groups "Status lines, the Prompt and Progress readings" and ends "they always paint, from any context, child or root, subject only to Status Visibility." The Prompt path (`ConsoleContext.cs:66-72`, the cited evidence) contains no visibility test at all, as FR-3.24 states explicitly; only Status and Progress are subject to Status Visibility. The trailing clause is unsupported by the cited lines for one of the three items it covers.

**Required fix:** Reword to "…they always paint, from any context, child or root; Status lines and Progress readings are subject to Status Visibility, the Prompt is not (FR-3.24)".

---

