# Verification findings for S12-traceability.md

1 finding(s). Apply every one.

## 1. [MINOR] §12.2 Source file → owning feature, rows for `Directory.Packages.props` (line 8154), `src/Directory.Packages.props` (8155) and `NuGet.config` (8156)

**Problem:** Three of the 27 rows misstate the Lines column, and each is contradicted by the PRD's own citations elsewhere. Both `Directory.Packages.props` and `src/Directory.Packages.props` are listed as 21 lines; both files are 19 lines, and §7.11 FR-11.43 itself cites them as `Directory.Packages.props:1-19` and `src/Directory.Packages.props:1-19`. `NuGet.config` is listed as 24 lines; the file is 22 lines, and both §2.4 and §7.2 cite `NuGet.config:5-22`. (Membership is correct: all 27 tracked files appear exactly once, with no extras and no omissions.)

**Required fix:** Change the Lines cells to 19, 19 and 22 respectively.

---

