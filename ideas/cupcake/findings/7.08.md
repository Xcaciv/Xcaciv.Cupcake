# Verification findings for 7.08-F4-package-search.md

13 finding(s). Apply every one.

## 1. [CRITICAL] §7.2 AC-2.15, §7.7 AC-7.26, §7.10 AC-10.5 (contradicted by §7.8 AC-8.27)

**Problem:** Four acceptance criteria make mutually exclusive claims about the same observable, so no implementation can satisfy the PRD's own QA suite. AC-2.15 ("a Session Variable dump afterwards shows an entry PACKAGESOURCEURL with an empty value"), AC-7.26 ("an empty value is left recorded under the uppercased key PACKAGESOURCEURL, visible in a subsequent variable dump") and AC-10.5 ("a subsequent Session Variable dump lists an entry under the upper-cased endpoint key") assert the pollution is visible; AC-8.27 asserts "the Session's store is unchanged and the endpoint key is not listed" and explicitly flags the conflict. The source settles it against the three: the Lit Shell registers the Command as `Controller.AddCommand("internal", new SearchCommand())` (src/Xcaciv.Cupcake.Lit/Program.cs:11) with `modifiesEnvironment` defaulting to false (OUT-OF-REPO CommandController.cs:190); the executor runs the Command against `environmentContext.GetChild(...)` — a fresh store seeded with a copy (EnvironmentContext.cs:45-55) — and merges back only under `if (commandDescription.ModifiesEnvironment && childEnv.HasChanged)` (CommandExecutor.cs:216). The store-on-read side effect therefore lands in the discarded child scope and never reaches the Session. A clone team following AC-2.15 would have to register the PACKAGE group as environment-modifying, which by §7.1 FR-1.21 stamps the flag on the whole group and also changes Package Install's write-back — a behavioural divergence introduced by the PRD itself.

**Required fix:** Rewrite AC-2.15, AC-7.26 and AC-10.5 to match AC-8.27: the read stores the empty default in the per-execution child scope only, and a subsequent variable dump does NOT list PACKAGESOURCEURL, because the Command is registered without the environment-modifying flag. Re-scope the supporting requirements the same way — FR-2.37, FR-7.69, FR-10.4 and UC-2.3 E3 and the UC-10.A postcondition must say "in the Command's own child scope, discarded on return", not "in every subsequent Session Variable dump". Delete the "the project quirk register predicts the opposite, and this criterion is the decision point" clause from AC-8.27 once the other three agree, and correct orchestrator-notes D7, which is the origin of the error.

---

## 2. [CRITICAL] §6 GR-26 (contradicted by §6 GR-27, §7.8 FR-8.16, §7.1 FR-1.68)

**Problem:** GR-26 states as a product-wide rule that the read-side-effect is observable: "a mere read can create an entry that a later variable dump lists — the first Package Search stores an empty value under the Registry Endpoint key — and the echo Built-in Command leaves an empty entry behind for every `%NAME%` it fails to resolve." The very next requirement, GR-27, gives the rule that disproves both examples: values flow back to the caller's scope only when the Command's registration record declares it environment-modifying. Package Search is registered with that flag off (§7.8 FR-8.4; `Lit/Program.cs:11` calls AddCommand with the default `modifiesEnvironment: false`), and SAY is not environment-modifying either (§7.1 FR-1.68 says so explicitly: "It is invisible for `SAY`"). Framework evidence confirms the gate: `CommandExecutor.cs` writes back only under `if (commandDescription.ModifiesEnvironment && childEnv.HasChanged)`. §7.8 FR-8.16 is the correct statement — the write lands in the discarded per-execution child scope and the Session store is unchanged.

**Required fix:** Rewrite GR-26's consequence clause to: "a read creates an entry **in the reading Command's own child scope**; whether it survives is decided by GR-27, so for a Command not declared environment-modifying — which includes Package Search and the echo Built-in — the entry is discarded with the child and no later variable dump lists it. The side effect is observable only inside an environment-modifying Command, or when a host invokes a Command directly against its own store with no child scoping." Keep the mutating-read rule itself; only the persistence claim is wrong.

---

## 3. [CRITICAL] §7.2 FR-2.37 and AC-2.15 (also §7.2 UC-2.3 E3, §7.10 FR-10.4, §8 DM-3 Lifecycle, §11.2 Q-10) — contradicted by §7.8 FR-8.16

**Problem:** Five locations assert the disproved consequence as fact, and one of them is an executable acceptance criterion a clone would be tested against. §7.2 FR-2.37: "an entry `PACKAGESOURCEURL` with an empty value appears in every subsequent Session Variable dump"; §7.2 AC-2.15: "**and** a Session Variable dump afterwards shows an entry `PACKAGESOURCEURL` with an empty value"; §7.10 FR-10.4: "shall store an empty value under it as a side effect, after which a Session Variable dump lists it"; §8 DM-3: "the first Package Search **permanently** adds an empty `PACKAGESOURCEURL` entry that a subsequent variable dump then lists" — which the same paragraph then contradicts three sentences later with its own write-back rule; §11.2 Q-10 records it as a quirk of "the variable store" and attributes it to "6, 7.2" only. §7.8 FR-8.16 is correct: the child scope is discarded because Package Search is registered as not modifying Session Variables. A clone built to AC-2.15 must either wire write-back for a non-modifying Command or ship a test its faithful implementation fails.

**Required fix:** Correct all five. FR-2.37 and §7.10 FR-10.4: end at "...writes the default back into the Command's per-execution child scope, which is then discarded because Package Search is not registered as environment-modifying; the Session's store is unchanged." AC-2.15: replace the second clause with "**and** a Session Variable dump afterwards shows **no** `PACKAGESOURCEURL` entry — the write landed in the discarded child scope; the write is observable only when a host invokes the Command directly against its own store." §8 DM-3: delete "permanently" and the dump clause, and cross-reference the write-back sentence in the same paragraph. §11.2 Q-10: restate as "a read mutates the Command's child scope (invisible in the Session today, and visible the moment a Command is declared environment-modifying)" and add §7.8 to its § column.

---

## 4. [MAJOR] §11.2 — no rows for the product-wide reporting quirks; body at §6 GR-46 and GR-47, §7.6 FR-6.45/FR-6.48/FR-6.49, AC-6.22/AC-6.23/AC-6.25, §7.10 FR-10.24/AC-10.7/AC-10.11, §7.8 FR-8.48/FR-8.49/AC-8.22, §7.2 FR-2.52/FR-2.55/AC-2.6

**Problem:** Four product-wide QUIRK-tagged behaviours have no register row. §6 GR-46 ("The product shall silently correct invalid values rather than reporting them") and GR-47 ("Work the product does not perform… shall be announced as ordinary Output, never as failures") are stated as product-wide requirements with their own keep-or-fix framing, and §7.6 FR-6.49 says registry diagnostics are "discarded by construction" so "a Registry outage and a zero-result search are indistinguishable" — an availability defect Q-7 (zero results print nothing) does not cover. Q-7's disposition "emit an explicit no matches" is unimplementable without also deciding the outage case.

**Required fix:** Add:
| Q-73 | Invalid values are silently corrected rather than reported: a Result limit outside 1–100 is clamped, a search term over 200 characters is truncated to 200, and a blank search term returns empty Output and issues no query — none produces any message. | 6, 7.6, 7.8, 7.10 | U | **Decide** with Q-7 — reporting these is more helpful and less faithful. |
| Q-74 | Work the product does not perform is announced as ordinary Output, never as a failure, and never affects exit status: Package Install answers `Not installing ` plus its comma-joined arguments, and a piped Package Search answers `Unsupported search method for <chunk> (piped)` immediately followed by its comma-joined arguments with no separator, so they run into the closing parenthesis. | 6, 7.2, 7.6, 7.8 | U | **Fix** — route refusals to the failure channel and add the missing separator. |
| Q-75 | Registry-side diagnostics are discarded by construction: the Package Registry Client accepts a diagnostic sink at every entry point and every call site in the product passes the discard sink, so a Registry outage, a feed error and a genuine zero-result search are indistinguishable to the user. | 7.6, 7.7 | S | **Fix** — a supply-chain client that cannot report transport failure has no failure signal at all. |
| Q-76 | The No Plugins Available guidance travels on the Output channel rather than the Status channel, so a Shell started with Status Visibility off suppresses `Loading Commands` but still prints the guidance, and the guidance renders in Output colours. | 7.2, 7.3 | D | **Fix** — put narration on one channel. |

---

## 5. [MAJOR] §11.2 — no rows for FR-6.31 or AC-8.15; body at §7.6 FR-6.31, §7.8 FR-8.25/AC-8.15 ("QUIRK pin")

**Problem:** Two QUIRK-tagged behaviours with observable consequences have no row. §7.6 FR-6.31: "A blank or whitespace-only configured Plugin Directory is a fatal startup failure, not the benign branch" — a Shell Operator who clears the setting to disable plugins kills startup instead. Q-11 covers only the present-but-empty *directory*, not a blank setting *value*. §7.8 AC-8.15 pins that the Result limit bounds records, not output lines, so `-take 1` at the full detail level produces six lines; §7.8 lists this as its tenth keep-or-fix decision.

**Required fix:** Add:
| Q-80 | A blank or whitespace-only configured Plugin Directory is a fatal startup failure rather than the benign No Plugins Available branch, because registration rejects it by raising and the raise is not the benign type. Reachable only for an Operator who overrides the setting. | 7.6 | U | **Fix** — an emptied setting should mean "no plugins", not "do not start". |
| Q-81 | The Result limit bounds records, not output lines: `-take 1` at the full detail level produces six lines. Whether the limit was meant as a record count or an output-size control is not determinable from the repository. | 7.8 | D | **Decide** — record count is the defensible reading; state it. |

---

## 6. [MAJOR] §7.8 FR-8.16 ("the discrepancy is routed to open questions") vs §11.1

**Problem:** FR-8.16 defers the conflict it correctly identifies: "the project's quirk register records the stronger consequence that a later variable dump lists the polluted key; that consequence does not follow from the observed discard rule for a non-modifying Command, and the discrepancy is routed to open questions." §11.1 contains OQ-1 through OQ-25 and none of them is this question; §11.2 Q-10 instead records the disproved version as settled fact. The routing promised in the deliverable was never performed, so the only place the conflict is flagged is a parenthetical inside one feature subsection.

**Required fix:** Resolve it rather than route it — the framework evidence settles it (see the two findings above). Delete the "*Unresolved:* … routed to open questions" sentence from FR-8.16, keep the substantive statement, and correct §11.2 Q-10. If the team prefers to defer, add an actual OQ-26 to §11.1 naming both readings and the framework line (`CommandExecutor.cs` write-back guard) that decides it, and make FR-8.16 cite it by number.

---

## 7. [MINOR] §8 DM-7 ("**QUIRK (Conflict B).**") and §7.8 UC-8.D error flow D-E1 ("INFERRED (Conflict D).")

**Problem:** Both cite a conflict register — "Conflict B", "Conflict D" — that appears nowhere in the document. There is no Conflict A, C or any key defining the scheme; a whole-document search returns only these two uses. They are leftover synthesis-phase markers (§12.4 mentions "conflicts resolved" during synthesis but the register itself was not carried into the deliverable), and a reader cannot tell whether an unresolved disagreement is being flagged.

**Required fix:** Delete both parentheticals. Both claims are settled elsewhere — DM-7's by §7.8 FR-8.38/FR-8.39 and §6 GR-22/GR-23, and D-E1's by §7.1 FR-1.51/FR-1.52 — so replace "(Conflict B)" with a cross-reference to §7.8 FR-8.39 and "(Conflict D)" with one to §7.1 FR-1.52.

---

## 8. [MINOR] §7.8 Source notes, OUT-OF-REPO entry `src/Xcaciv.Command/CommandExecutor.cs:222-228` (evidence for FR-8.54, FR-8.55)

**Problem:** The entry is cited for three things — "the generic failure Output line, the `**Error: ` Status prefix, and trace routing". At framework v2.1.2 only the Output line is inside the cited range (`:228`); the `**Error: ` status prefix is at `:229` and the trace write at `:230`. Two of the three claims fall outside the citation. §7.7 FR-7.79 cites the same evidence correctly as `:223-231`.

**Required fix:** Change the citation to `src/Xcaciv.Command/CommandExecutor.cs:224-231` (or `:228-230`) so it actually spans the catch block's three writes.

---

## 9. [MINOR] §7.8 Source notes, OUT-OF-REPO entry `AbstractCommandParameter.cs:16-20` (evidence for FR-8.53)

**Problem:** Cited for "placeholder metadata defaults". Lines 16-20 of `src/Xcaciv.Command.Interface/Attributes/AbstractCommandParameter.cs` at v2.1.2 are a doc comment plus the opening of the `Name` property getter. The placeholder defaults are at `:11` (`protected string _helpName = "TODO";`) and `:15` (`public string ValueDescription { get; set; } = "";`), both outside the cited range.

**Required fix:** Change to `AbstractCommandParameter.cs:11, 15` (optionally adding `:19-23` for the name-normalising setter, which is what lowercases the declared parameter names).

---

## 10. [MINOR] §7.8 Source notes, OUT-OF-REPO entry `src/Xcaciv.Command.Interface/CommandDescription.cs:18, 24, 52-64, 69-79`

**Problem:** The entry is cited for "command-name normalisation and the argument tokenizer's match-and-sanitize rules (FR-8.2, FR-8.32, FR-8.52)". `:18` (InvalidCommandChars) and `:52-64` / `:69-79` are correct, but `:24` is a `/// <summary>` doc-comment line. The parameter sanitize regex the claim depends on — `InvalidParameterChars = new Regex(@"[^-_\da-zA-Z .*?\[\]|\"\"~!@#$%^&*\(\)]+")` — is at `:23`, one line earlier.

**Required fix:** Change `:24` to `:23`.

---

## 11. [MINOR] §7.8 UC-8.A mermaid flowchart (nodes A → B → C)

**Problem:** The diagram evaluates "Upstream Pipeline stage attached?" first and only reaches "Help spelling present?" on the no-pipe branch. The framework does the opposite: `CommandExecutor.ExecuteAsync` tests `_helpService.IsHelpRequest(ioContext.Parameters)` at `CommandExecutor.cs:58` unconditionally, before any command instance exists, and pipeline stages reach that same method (`CommandController.cs:236` → `ExecuteCommandInternal` → `:275` `_commandExecutor.ExecuteAsync`). The piped-versus-direct split only happens later, inside `AbstractCommand.Main` at `:154`. So `something | PACKAGE SEARCH --help` prints the `Package` group's member listing rather than the per-chunk `Unsupported search method …` lines, the opposite of what the diagram predicts. This also makes FR-8.46's "solely" true only within `Main`, not for the dispatch as a whole.

**Required fix:** Swap nodes B and C in the flowchart so the help-spelling check precedes the pipe check, and add a sentence to FR-8.46 noting that the Command Service's help interception (FR-8.51) runs ahead of the piped/direct routing and therefore wins even when an upstream stage is attached.

---

## 12. [MINOR] §7.8 FR-8.16 (the *Unresolved* sentence)

**Problem:** FR-8.16 says the discrepancy with the project's quirk register "is routed to open questions". No open question or quirk-register row in `prd-sections/S11-open-questions.md` records it: OQ-1 through OQ-25 contain nothing about the variable-store side effect, and quirk row Q-10 records only the write itself ("Reading the Registry Endpoint setting writes an empty value back into the variable store as a side effect"), is scoped to §6 and §7.2 rather than §7.8, and does not mention the dump-visibility disagreement at all. The cross-reference is therefore unsupported, and AC-8.27's "this criterion is the decision point" has no matching entry to resolve.

**Required fix:** Either add an explicit open question in S11 ("Does the empty `PACKAGESOURCEURL` written on a missing-key read ever reach the Session's own store? Quirk register D7/Q-10 says yes; the write-back gate at `CommandExecutor.cs:216-219` says no for a non-modifying Command") and cross-reference it by number from FR-8.16 and AC-8.27, or delete the "routed to open questions" claim.

---

## 13. [MINOR] §7.8 Source notes, `Directory.Packages.props:7-10, 15-17`

**Problem:** Cited as the evidence for "pinned dependency versions", and the §7.8 External-technology test bullet claims "xUnit 2.9.3 with the Microsoft test SDK and a coverage collector". The coverage collector (`<PackageVersion Include="coverlet.collector" Version="6.0.4" />`) is at `Directory.Packages.props:14`, outside the cited `15-17` range, so that third dependency has no citation. §7.7 cites the same evidence correctly as `:14-17`.

**Required fix:** Change the range to `Directory.Packages.props:7-10, 14-17`.

---

